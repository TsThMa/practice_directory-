#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (success)
 */
func getSquare(num)
{
	if (num is a number)
	{
		return (num**2) /*this means number o the power of 2*/
	}
	print("please enter a valid number")
	return;
}
