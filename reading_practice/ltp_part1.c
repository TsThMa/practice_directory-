#include <stdio.h> /* incl. header def standard (std) inputp (i) and output (o) */

/**
 * main - Entry point (means that main function is the entry point to the programme)
 * 
 */
int main(void) /* void in brackets tells us that there is no argument/parameter passed to the function */
{
printf("Hello World!\n"); /*prints theh words in the bracets to "standard output" */
return 0; /* integer (int) 0 is returned by programme, as main function sugg. that "int" is the data type returned to*/
}
